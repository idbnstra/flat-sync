[project]
name = "flat-sync"
version = "0.1.0"
description = "A tool for aligning historical and modern panoramic images."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "streetview-dl>=0.7.0",
    "fastapi>=0.110.0",
    "uvicorn>=0.27.0",
]
